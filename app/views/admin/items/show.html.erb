<div class="h3 pb-2 mb-4 text-success border-bottom border-success">
  商品詳細
</div>
<div class="container">
  <div class="row g-0">
    <div class= "col-md-7">
      <%= image_tag @item.image, size:'400x300' %>
    </div>

    <div class="col-md-3">
      <div class= "card-body ">
        <p class="card-text">商品名
        <%= @item.name %></p>

        <p class="card-text">商品説明
        <%= @item.introduction %></p>

        <p class="card-text">ジャンル名
        <%= @genre.name %></p>

        <p class="card-text" >税込価格<br>(税抜価格)
        &yen;<%= @item.with_tax_price %>（&yen;<%= @item.price%>）</p>


        <p class="card-text">販売ステータス
        <% if @item.is_active %>
          <%=  "販売中" %>
        <% else %>
          <%= "販売停止中" %>
        <% end %>
        </p>
        <%= link_to"編集する", edit_admin_item_path(@item.id), class: 'btn btn-sm btn-success' %>
      </div>
    </div>
  </div>
</div>