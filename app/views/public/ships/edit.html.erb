</h3>配送先編集<h3>
  
<%= form_with model: @ship,url: '/public/ships', method: :patch  do |f| %>

  <h4>郵便番号（ハイフンなし）</h4>
  <%= f.text_field :post_code %>
  
  <h4>住所</h4>
  <%= f.text_field :address %>
  
  <h4>宛名</h4>
  <%= f.text_field :name %>
  
  <%= f.submit '保存' %>
  
<% end %>